name: Windows Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install project and build tools
        run: python -m pip install -e ".[dev,build]"

      - name: Run tests
        env:
          PYTEST_DISABLE_PLUGIN_AUTOLOAD: "1"
        run: python -m pytest -q

      - name: Build single-file Windows executable
        shell: pwsh
        run: ./scripts/build_windows.ps1 -SkipTests

      - name: Create release archive
        shell: pwsh
        run: |
          Compress-Archive `
            -Path .\dist\CodexUsageDashboard.exe, .\README.md `
            -DestinationPath .\dist\CodexUsageDashboard-windows-x64.zip `
            -Force

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v7
        with:
          name: CodexUsageDashboard-windows-x64
          path: dist/CodexUsageDashboard-windows-x64.zip
          if-no-files-found: error

  publish-release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build-windows
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download Windows artifact
        uses: actions/download-artifact@v8
        with:
          name: CodexUsageDashboard-windows-x64
          path: release

      - name: Publish tagged GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF_NAME}"
          archive="./release/CodexUsageDashboard-windows-x64.zip"
          if gh release view "$tag" >/dev/null 2>&1; then
            gh release upload "$tag" "$archive" --clobber
          else
            gh release create "$tag" "$archive" --generate-notes --title "Codex Usage Dashboard $tag"
          fi
